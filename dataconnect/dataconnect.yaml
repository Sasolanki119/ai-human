specVersion: "v1beta"
serviceId: "ai-human"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "ai-human-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
